# Inference Service dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27.0

# API mode (LiteLLM covers Anthropic, OpenAI, Together.ai, Groq, and 100+ others)
# Only required when INFERENCE__MODE=api
litellm>=1.34.0

# gRPC server (stubs generated from shared/proto/inference.proto at build time)
grpcio>=1.62.0
grpcio-tools>=1.62.0
grpcio-health-checking>=1.62.0
