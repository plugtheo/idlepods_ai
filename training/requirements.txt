fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
pydantic-settings>=2.0
# LoRA training stack — only needed at runtime when a training job runs
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.19.0
accelerate>=0.29.0
trl>=0.8.1
# Unsloth: 2-5x faster LoRA training via fused CUDA kernels.
# Installed via Dockerfile RUN step (after torch) for proper CUDA wheel selection.
# Listed here for documentation; the Dockerfile install command takes precedence.
